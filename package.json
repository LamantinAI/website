{
  "name": "lamantin-site",
  "private": true,
  "type": "module",
  "description": "The Lamantin AI site. Node 24 runs the TypeScript directly — there is nothing to install.",
  "engines": { "node": ">=22.6" },
  "scripts": {
    "build": "node --experimental-strip-types src/build.ts",
    "serve": "python3 -m http.server 8830"
  }
}
